_format_version: "3.0"
_transform: true

services:
 - name: "go-finance-service"
   host: "go-finance-service"
   protocol: "http"
   path: '/'
   routes:
     - name : "finance-service"
       paths:
         - "/finance-service"
       strip_path: true
